# 掼蛋项目完整审阅报告

> 审阅对象：`develop` 分支 @ `e1d8178`（v0.8.1-beta.2），Python 3.10.20 / macOS arm64
> 审阅范围：需求（README、`docs/rules.md`、`docs/ai.md`、ADR、路线图）与实现（`engine/`、`ai/`、`gui/`、`tui/`、`ui/`、`storage/`、`tests/`、CI、打包）
> 方法：逐行阅读 + 5 路并行独立审计 + **对每条高优先级结论做独立复现验证**（见附录 A「已证伪／降级的结论」）

---

## 0. 结论摘要

**规则引擎是这个项目最扎实的部分**：`docs/rules.md` 中的每条口径我都做了行为验证，全部通过（级牌强度、连牌自然点、同花顺↔炸弹层级、四王最大、升级 3/2/1、过 A 双上、冲 A 失败）。30 局随机对局中「三游手牌较少」不变量 0 违反，ruff/mypy 全绿，429 项测试 27.7s 全部通过。**引擎层没有发现规则错误。**

**主要风险集中在四类「文档承诺 = 代码事实」的偏差，以及若干可复现的数据丢失路径：**

| # | 问题 | 级别 | 后果 |
|---|---|---|---|
| 1 | 贡还牌阶段关闭程序 → 整局静默丢失 | **P0** | 用户失去一整局进度，无任何提示 |
| 2 | `profile.json` 损坏后统计静默归零，下次写入即覆盖原件 | **P0** | 战绩统计永久丢失且无感知 |
| 3 | TUI 回放异常未捕获 → 整个 TUI 崩溃退出 | **P1** | 点一次「回放」即退出程序 |
| 4 | 文档声称的搜索强度不成立（MCTS 只覆盖 54-62% 决策；64/96 次模拟实际只有 22-52 次；`iterations` 在预算下几乎是死旋钮） | **P1** | AI 强度宣称与实测不符，评测口径失真 |
| 5 | 时钟预算系统性超支（p50 241ms > 240ms 上限） | **P1** | 延迟门禁测的是溢出量而非算法成本 |
| 6 | 三条写入非原子 + 存档/历史/统计各自为政 | **P1** | 崩溃窗口内产生永久不一致 |
| 7 | `tribute_state` 字段写入但回放不重建 → 含该字段的存档被判「损坏」 | **P1** | 潜在存坏用户存档 |
| 8 | 唯一存档槽在 4 处被无归属校验地删除（并行运行时互相覆盖） | **P1** | 并行场景下未完成对局丢失 |
| 9 | 存储原语零直接测试；GUI 结算测试用内存替身替掉真实加锁事务 | **P2** | 上述存储缺陷得以同时存活 |
| 10 | 测试存在恒绿断言（`test_card.py:25-31` 永不失败）、无覆盖率门槛、CI 未接线 benchmark 门禁 | **P2** | 回归保护有缺口 |

**一句话**：引擎可信、存储设计方向正确但边界处理不足、AI 的真实形态与文档叙述差距最大、测试数量充足但**鉴别力**不足。

---

## 1. 客观基线

```
ruff check src tests scripts   →  All checks passed
mypy src                       →  Success: no issues found in 76 source files
pytest -q                      →  429 passed in 27.69s
```

| 指标 | 实测值 | 说明 |
|---|---|---|
| 源码规模 | ~10.2k 行（src），总 ~22.2k 行（含测试） | `gui/window.py` 单文件 1667 行 |
| 引擎规则回归 | 全通过 | 见 §2 |
| 生产 MCTS 实际模拟数 | 档 3：mean 32、min 22、max 52；档 4：mean ~40 | 配置标称 64 / 96 |
| MCTS 实际覆盖率 | 档 3：54%；档 4：62% | 其余决策回落 `AdvancedStrategy` |
| 单步决策延迟 | 档 3：mean 149ms / p95 247ms；档 4：mean 279ms / p95 430ms | 预算 240 / 420ms |
| 预算超支比例 | 档 3：50% 决策超 240ms；档 4：55% 超 420ms | 系统性问题，非抖动 |
| 真实人机体验 | 每轮等待 3 家 AI ≈ 0.45s（档 3）/ 0.84s（档 4） | 可接受，无需优先优化 |

---

## 2. 需求核对：规则与 AI 口径

### 2.1 规则引擎（**全部通过**）

对 `docs/rules.md` 逐条做行为验证，以下是实测结果：

| 文档口径 | 验证 | 结果 |
|---|---|---|
| 级牌是本局最大非王牌 | `effective_rank(5, level=5) == 15 > A(14)`；王仍为 100/101 | ✅ |
| 级牌在连牌中不升为 A 之上 | `3-4-5-6-7 @ level=5` → `comparison_rank == 7`（未被提升） | ✅ |
| 级牌对子压 A 对子 | `55 > AA @ level=5`；王对 > 级牌对 | ✅ |
| 同花顺可压 4 张/5 张普通炸弹 | `SF vs 5-bomb = True`，`SF vs 4-bomb = True` | ✅ |
| 6+ 普通炸弹可压同花顺 | `5-bomb vs SF = False`（符合文档） | ✅ |
| 顺子固定 5 张（9 + A2345 = 10 窗口） | 窗口枚举计数正确 | ✅ |
| 三连对固定 3 对（11 + A23 = 12 窗口） | ✅ | ✅ |
| 钢板固定 2 组（12 + A2 = 13 窗口） | ✅ | ✅ |
| 升级 +3/+2/+1，仅头游方 | `compute_level_change` 6 项测试通过 | ✅ |
| 过 A 双上 / 冲 A 失败降回 2 | 整场比赛 7 局实测：级牌轨迹 13→14→(过A回2)→5→6→…→9，`winner_team=1` 正确 | ✅ |
| 「三游 = 剩余手牌较少者」 | 30 局随机对局，0 违反 | ✅ |

**唯一规则缺口**：`docs/rules.md:60`「不能跨越 K-A-2-3-4」在代码里被正确拒绝（`_try_straight` 只生成 `range(2,11)` 窗口 + `A2345`，不生成 `KA234`），但**没有任何测试**覆盖这条负例。

### 2.2 AI 口径（**与文档存在实质偏差**）

这是本次审阅**价值最高**的发现。`docs/ai.md` 与 README 对高段位 AI 的描述与实际运行形态不一致：

**(a) 档 3/4 大多数决策根本不走搜索**

```python
# src/guandan/ai/strategies/professional.py:143-148
def _should_use_mcts(self, state, player) -> bool:
    """M6 性能闸门：只在中后期或关键局面启用 MCTS。"""
    if state.hand_size(player) <= self.mcts_hand_threshold:   # 阈值 = 10
        return True
    opponent_min = opponent_min_cards(state, player)
    return 0 < opponent_min <= self.mcts_hand_threshold
```

实测整局统计：档 3 只有 **54%** 的决策由搜索产生，档 4 为 **62%**，其余直接回落 `AdvancedStrategy`——即**与档 2「高手」同一套贪心+估值逻辑**。而 `docs/ai.md:10-11` 写的是「档 3 职业：团队感知 SO-ISMCTS」、「档 4 戴长胜：更高搜索预算」。用户在大部分牌局里体验到的「职业 AI」其实不是职业 AI。

**(b) 标称迭代数在生产路径达不到**

`MCTS_CONFIG["iterations"] = 64`、`profiles/dachangsheng.json` 为 96。但生产用时钟预算截断：

```python
# src/guandan/ai/mcts/information_set.py:289-291
while simulations < iterations:
    if simulations > 0 and deadline is not None and time.perf_counter() >= deadline:
        break
```

实测同一批 12 个中残局位置：

| iterations（预算固定 240ms） | 平均完成模拟数 | 决策结果变化 |
|---|---|---|
| 32 | 29.2 | — |
| **64（仓库默认）** | **32.2** | 基准 |
| 96 | 32.3 | 与 64 相比 **12 个位置只有 1 个变化** |
| 160 | 32.2 | 同上 |

**`iterations` 在生产路径上几乎是一个无效旋钮**（从 64 调到 160，12 个局面只有 1 个决策改变）。同一批位置在**关闭预算**时，64 与 96 会产生 12 个中 4 个不同的决策——即 `--deterministic-search` 与生产模式在 **1/3 的决策上选择了不同的动作**。

后果：`README.md:181` 声称的「固定迭代回归 8/16」与「生产留出 21/40」是**两种不同搜索**的成绩，胜率不可互相代理；`benchmarks/README.md:54-57` 已诚实说明「latency 不比较」，但未说明**动作序列本身也不同**。

**(c) 根以下几乎没有统计共享，实际退化为「根动作扁平 MC」**

```python
# src/guandan/ai/candidates.py:42-49 —— 动作键包含精确牌张与花色
return (pattern.type, pattern.rank, pattern.length, pattern.wild_used,
        pattern.suit, _card_counter_key(tuple(pattern.cards)))
```

因为隐藏牌每轮重新采样，**跨世界命中同一个「精确牌张组合」键的概率极低**，加上 `information_set.py:347-348` 在新建节点处 `break`，导致：64 次模拟恰好创建 65 个节点（1 根 + 64 子），深度 2 的 29 个节点中 22 个只被访问 1 次。

于是 `max_depth=12/14`、`widening_c/alpha`、`prior_weight` 这些参数的**深度语义基本空转**——树实际上只有一层。这不是崩溃，但它意味着「团队感知 SO-ISMCTS」的搜索强度被高估了，也解释了为什么加大预算买不到强度。

建议：树内动作键改为**可观测打法键**（类型/点数/长度/wild_used + 张数，忽略花色），精确牌张只用于合法性判定；并在 `SearchResult` 中暴露真实树深与节点数，让评测能看见这一点。

**(d) 时钟预算被系统性超支**

`information_set.py:290` 只在**两次模拟之间**检查 deadline，而单次模拟（含 rollout）耗时 5-13ms 且不可中断，因此实际延迟恒等于「预算 + 最多一次模拟的溢出」。实测：

- 档 3：p50 = **241.2ms**（预算 240ms），50% 决策越界，max 257ms
- 档 4：p95 = **430ms**（预算 420ms），55% 决策越界

`benchmarks/ai-v2-style-tuning.json` 自身就印证了这一点：`production_holdout.candidate_p95_seconds = 0.428975 > 0.420`。**按文档声称的预算设置 p95 门禁必然失败**；README 只能用 0.50（比预算宽 19%）才通过。建议：deadline 下沉到 rollout 循环内部，或把门禁阈值显式定义为 `budget × 1.15`。

### 2.3 信息边界（**干净，是亮点**）

`docs/ai.md:17-27` 的「搜索不得读取对手真实手牌」经独立差分验证成立：把两名对手的手牌互换（公开信息完全不变）后，`information_set_search` 的 `action_key/visits/mean_value/availability` **逐位相同**。

软证据要求（`docs/ai.md:27`「所有合法归属始终保持非零概率」）也满足：`determinize.py:69-74` 的似然只有乘性惩罚并 `return max(0.05, likelihood)`，`_weighted_owner` 按正权重轮盘抽样。

**唯一泄漏点**是已死的兼容路径 `search.py::mcts_search`（完美信息 MCTS，读真实手牌），它在 `mcts/__init__.py:24` 仍被导出。生产无调用，但这是一个**接线的地雷**：任何人把它接回生产就会立刻产生作弊级 AI。建议删除或改名加显式警告。

---

## 3. P0：数据丢失（已逐条复现）

### P0-1 贡还牌阶段关闭程序 → 整局静默丢失

**复现结果**：

```
round finished: True   steps: 102   order: [2, 0, 3]
savegame after finished round: False          # 上一局结算时已被删除
prepare_next_game      -> True  | pending: True | new hands: [27, 27, 27, 27]
savegame on disk right after re-deal: False   # 新局 27 张已发，磁盘上没有任何存档
begin_next_game_tribute-> True  | pending: True
savegame now (tribute pending): False
savegame after save_unfinished(): False       # 关窗动作也救不回来
storage dir: ['.history.lock', '.profile.json.lock', '.savegame.json.lock', 'history', 'profile.json']
```

**根因**（两条叠加）：

```python
# src/guandan/ui/session.py:667  —— 结算时删除存档
delete_savegame(expected_game_id=self.game_id)

# src/guandan/ui/session.py:675-677 —— 但新局状态存不进存档
def save_unfinished(self) -> None:
    state = self.require_state()          # 已发牌的新局
    if state.finished or self.game_saved:
        return                            # ← 若 self.state 仍是已结束的上一局，直接返回
```

`prepare_next_game()` 把新局放在内存字段 `_pending_next_game` 里，`self.state` 在 `finalize_next_game()` 之前仍是**已结束的上一局**。于是 `save_unfinished()` 提前 return，磁盘上既没有上一局（已删除）也没有新局（从未写入）。

而 `README.md:6,13` 明确宣称「断点续局恢复已实现」。**在这一窗口内关闭窗口/断电，整局静默丢失，且没有任何提示。**

**建议**：把 `_pending_next_game.state` + `finish_order` + `human_choice.kind` 序列化进存档（例如 `resume_pending` 字段）；或最低成本方案——pending 期间**不删除**上一局存档，让 `save_unfinished()` 在 pending 时也能落盘。

### P0-2 `profile.json` 损坏 → 统计静默归零 → 下次写入覆盖原件

**复现结果**：

```
written: 5
after truncation load_profile -> {'total_rounds': 0, 'head_rounds': 0, ...}   # 静默归零
after 1 more round  -> total_rounds = 1                                     # 原件已被覆盖
corrupt copy kept anywhere? ['.profile.json.lock', 'profile.json']          # 无任何备份
```

**根因**：

```python
# src/guandan/storage/profile.py:71-73
except (KeyError, TypeError, ValueError):
    # 文件损坏，返回默认值
    return copy.deepcopy(DEFAULT_PROFILE)
```

`update_profile()`（`profile.py:95-102`）在同一把锁内做「读-改-写」，读到的却是**默认值**，于是把用户的历史统计原地覆盖成 `1/0/0`。触发场景包括：写入过程中断电（`jsonio.py` 只 fsync 文件、**不 fsync 父目录**，macOS 上 `rename` 可能未落盘）、用户手工编辑出错、旧 schema 嵌套类型异常。

**建议**：损坏时不冒充成功——把原件重命名隔离为 `.corrupt-<timestamp>`，记录警告，并逐字段容错（`int(source_stats.get(...))` 这类转换要包住）。

---

## 4. P1：正确性与健壮性

### P1-1 TUI 回放异常未捕获 → 整个 TUI 退出

**验证结果**：

```
IllegalPlayError MRO: ['IllegalPlayError', 'Exception', 'BaseException', 'object']
is subclass of ValueError: False
ReplayCursor(bad) -> IllegalPlayError (NOT caught by TUI!): not player's turn: 1 != 0
```

TUI 只捕获 `ValueError`：

```python
# src/guandan/tui/screens/history.py:139-142
try:
    self.app.push_screen(ReplayScreen(detail))
except ValueError as exc:
    self.app.push_screen(ErrorModal(str(exc), title="回放数据无效"))
```

而 `ReplayScreen.__init__` → `ReplayCursor` → `replay_event_states` → `play_pattern` 会抛 `IllegalPlayError`（不是 `ValueError` 子类）。`load_history_detail`（`storage/history.py`）**只反序列化、不校验可回放性**，所以一条能解析但重放不一致的历史记录足以让异常逃出 Textual 消息处理器，导致 App 退出并打印 traceback。

GUI 侧做对了（`gui/window.py:691-694` 显式归一化为 `ValueError`）——这正是「同一共享层、两个前端行为分歧」的典型。

**建议**：TUI 照抄 GUI 的异常归一化，并在 `HistoryScreen` 兜 `Exception`。

### P1-2 存档写入的三段操作非原子

```python
# src/guandan/ui/session.py:641-667（顺序执行，三把独立锁）
save_history(...)        # ① 历史
update_profile(...)      # ② 统计
delete_savegame(...)     # ③ 删档
```

在第 ①/② 步之间被 kill 或断电，会出现「历史里有这一局、统计里没有」的永久不一致；`recorded_game_ids` 幂等键只在成功写入后记账，因此重启后**不会自愈**。

> **一处已证伪的初判**：初步审计称「`by_difficulty` 值类型异常会抛 `AttributeError` 中止事务」。我在磁盘上直接写入 `"by_difficulty": {"2": 5}` 后复现，实测 `update_profile` **正常返回**——`_migrate_profile` 会把非 dict 的嵌套值归一化掉（`{"2": 5}` → `{"2": {"matches":1,"wins":1}}`），`total_rounds` 等既有统计保留。**该路径不会中止事务**；真实代价只是那一条 `by_difficulty` 历史被重建。

**建议**：写一个「待结算」标记（pending settlement），启动时对账补记。

### P1-3 `tribute_state` 破坏存档往返

```python
# src/guandan/storage/savegame.py:180-182
replayed = replay_events(events)
if replayed != restored:                     # 全字段 dataclass 相等
    raise ValueError("savegame state does not match its event stream")
```

`replay_events`（`engine/replay.py`）**从不设置** `tribute_state`，而 `_dict_to_state` 会把存档里的值读回来。因此任何 `tribute_state` 非默认值的存档都会被判为「损坏」，GUI 只提供一个「删除存档」按钮。

今天引擎不写这个字段所以不可达，但 `savegame.py:366` 在写、`:396` 在读、`state.py:96` 定义了它——**一个字段三处不一致**。任何未来的写入方（例如修 P0-1 时把 pending 状态存进去）都会立刻踩中。

**建议**：删掉死字段，或在 `replay.py` 中显式重建它；并且**不要把「全等」当作损坏判据**——应改为「不一致时信任快照 + 记录警告」。

### P1-4 TUI 贡还牌定时器缺少「当前屏」守卫

```python
# src/guandan/tui/screens/game.py:853-866
def _begin_next_game_tribute(self) -> None:
    ...
    if not self.session.is_next_game_pending():
        return                      # ← 只判断 session 状态，不判断"当前是否本屏"
    started = self.session.begin_next_game_tribute()
    ...
    if self.session.pending_next_game_choice() is not None:
        self.call_after_refresh(self._show_tribute_choice)
        return
    self._finish_next_game(None)
```

GUI 在 4 处做了 `if self._main_window.stack.currentWidget() is not self: return` 守卫（如 `window.py:1441-1447`），TUI **一处都没有**；`action_rules`（`game.py:893-896`）也没有任何守卫，可以在 0.5s 窗口内把 `RuleScreen` 压到栈顶。

> **一处已更正的初判**：初步审计称此路径会让「引擎替玩家自动选贡/还牌」。我核对了回调链——当人类需要决策时，`_show_tribute_choice` 会弹出 `TributeChoiceModal`，而 `_finish_next_game(None)` 在 `pending_next_game_choice() is not None` 时走的是 `cancel_next_game()`（`game.py:880-882` → `session.py:531-537`），**是取消而非代选**。

**修正后的实际影响**（P2 级）：规则屏打开期间 (a) 贡还牌弹窗可能被压到规则屏背后、(b) 不需要人类决策时会静默推进新一局。是屏幕上下文缺陷，不是规则代选。

**建议**：`_begin_next_game_tribute` / `_maybe_ai_turn` 开头加 `if self.app.screen is not self: return`（保留 pending，回到本屏后再触发），`action_rules` 加 `_ai_running or is_next_game_pending()` 守卫——与 GUI 的 4 处对齐。

### P1-5 评价体系内部不一致

- **调参评分混入墙钟延迟**：`tuning.py:95-106` 的 `latency_penalty = max(0, p95 - target) * 0.10` 与 `docs/ai.md:82`「固定迭代避免 CPU 负载改变候选排序」直接矛盾。实测该 p95 距阈值只有 7% 余量（0.465s vs 0.500s），慢 8% 的机器就会翻转候选排序。
- **留出集二次利用**：`tuning.py:178-192` 在 final 留出种子上挑选最优、又用同一批种子报告成绩；`ai-v2-style-tuning.json` 中入选与亚军只差 3 局（10/20 vs 7/20）。选择偏差与噪声同量级。
- **benchmark 不校验完成状态**：`benchmark.py:348` 用 `state.finish_order[0]` 判定胜队，未过滤 `finished`；实测 `run_match(5001, max_turns=60)` 得到 `finished=False, winner_team=0`。arena 侧有 `candidate_won` 的 `finished` 保护，benchmark 侧没有。
- **门禁漏掉炸弹指标**：CI 的 `--compare` 只看 completion/duration/turns。实测当前代码对比检入基线：`avg_bombs` 从 `[0.25, 0.45]` 涨到 `[1.0, 1.0]`（**3-4 倍**）、队伍胜场从 `[3,17]` 变为 `[5,15]`，**门禁依然 pass**。

**建议**：删除调参中的墙钟项（或降级为硬约束）；种子三分（screen / select / confirm），报告只用 confirm；benchmark 补 `finished` 过滤；把 `average_bombs`、`team_wins` 纳入门禁阈值。

### P1-6 `--deterministic-search` 与生产是两种搜索

见 §2.2(b) 实测：关闭预算时 64 vs 96 在 12 个位置中改变 4 个决策；生产预算下只改变 1 个。因此 `README.md:181` 的「固定迭代回归 8/16」不能作为生产强度的代理指标。另外 `arena.py:59-68` 只测 difficulty 0，`fixed_iteration_strategy_factory` 的 64/96 分支**从未被测试执行**。

### P1-7 存储层的三个已复现缺陷（跨进程锁专项）

`README.md:17` 宣称「GUI / TUI 并行运行时使用跨进程存储锁保护 Profile、存档和历史写入」。锁的选型是对的（进程内缓存同一 `FileLock` 实例 → 同线程可重入不死锁；进程死亡时 flock 由内核释放 → 无陈旧锁），但以下三点实测成立：

**(a) 唯一的存档槽在 4 处被无归属校验地删除**

`delete_savegame()` 提供了正确的防误删原语：

```python
# src/guandan/storage/savegame.py:257-273
def delete_savegame(*, expected_game_id: str | None = None) -> bool:
    ...
    if expected_game_id is not None:
        ...
        if stored_game_id != expected_game_id:
            return False        # ← 只删属于本次会话的存档
```

但 5 个调用点里只有 1 个用了它：

```
src/guandan/ui/session.py:667        delete_savegame(expected_game_id=self.game_id)   ← 正确
src/guandan/tui/screens/load_save.py:126   delete_savegame()                          ← 无校验
src/guandan/tui/screens/main_menu.py:211   delete_savegame()                          ← 无校验
src/guandan/gui/window.py:531              delete_savegame()                          ← 无校验
src/guandan/gui/window.py:1621             delete_savegame()                          ← 无校验
```

`storage/paths.py` 只有单一 `savegame.json` 槽位，`save_game` 无条件覆盖。因此 `README.md:17` 宣称的并行运行能力实际只覆盖「写坏」，**不覆盖「互相覆盖」**：TUI 弹出「已有未完成存档，是否覆盖？」的确认框、用户犹豫的这几十秒里，并行运行的 GUI 每批 AI 后都在自动保存（`session.py:693-704`），用户一点确认就把对方刚写的进度删掉。

**建议**：弹框时记下 `game_id`，删除时传 `expected_game_id`；`save_game` 增加 owner/lease 或分槽策略。

**(b) `os.replace` 会把符号链接替换成普通文件**（`storage/jsonio.py:28`）

```
before: is_symlink = True
after : is_symlink = False | real file still: {"from": "real"}
```

如果用户把 `~/.guandan/profile.json` 软链到同步/备份目录（Dropbox/OneDrive 场景很常见），第一次保存后链接就被普通文件取代，**同步目录里的数据从此不再更新**且无人察觉。

**(c) 跨线程锁争用被误报成「另一个掼蛋进程」，并冻结调用线程 5 秒**

```
LOCK_TIMEOUT_SECONDS = 5.0
waiter blocked 5.03s then raised: 存储正在被另一个掼蛋进程使用：contend.json
```

实际是**同一进程的另一个线程**（GUI 的 `QThreadPool` worker，`gui/window.py:1509`）。`locking.py:40-42` 把 `filelock.Timeout` 一律翻译成「另一个进程」，诊断信息误导；更实际的问题是 GUI/TUI 主线程会**最长冻结 5 秒**（TUI 的落库直接在事件循环线程：`tui/screens/game.py:937`）。

**(d) 原子写不 fsync 父目录**

`jsonio.py:26-28` 只对文件 `fsync`，随后直接 `os.replace`，**未 fsync 父目录**；macOS 上 `os.fsync` 也不含 `F_FULLFSYNC`。掉电后 rename 可能未落盘——这不会写坏文件（读者只会看到旧/新完整快照），但「刚刚明明保存了」可能回退到上一版，也是 P0-2 里 profile 被截断的成因之一。

**建议**：弹框时记下 `game_id`、删除时传 `expected_game_id`（4 处调用点）；写前 `path.is_symlink()` 则解析真实目标；锁等待改 `blocking=False` + 短重试并把存储 IO 移出 UI 线程，错误消息区分线程/进程；`os.replace` 后 `fsync` 父目录。

---

## 5. 架构与代码质量

### 5.1 共享层职责被反向污染

`ui/session.py` 定位是「frontend-neutral」，但里面塞进了 GUI 专属的显示策略：

```python
# src/guandan/ui/session.py:217-242
def table_display_actions(self, *, preserve_completed_trick: bool = False):
    """...The desktop GUI opts into ``preserve_completed_trick`` so a collected
    trick remains readable during the pause before the next lead.  The TUI
    keeps its compact, immediate-clearing behavior."""
```

同时 `table_display_actions()` 是**带副作用的渲染 getter**（写入 `self.displayed_table_actions` / `self.last_display_turn`），调用顺序会影响显示结果。建议把这两个字段与 `preserve_completed_trick` 迁回 GUI，session 只提供 `current_table_players()` / `locked_passed_players()` 原始数据。

### 5.2 `gui/window.py` 1667 行 God-class + 三份卡面绘制

`GamePage` 单类 513 行 / 30 个方法，同时负责视图构建、AI 调度、贡还牌对话框流水线、导航门禁；`GuandanMainWindow` 又反向穿透 `self.game_page.session` 直接操作 session 内部。

卡面绘制有三份独立实现（`cards.py:149-174` 的 `CardButton._paint_normal_card`、`cards.py:384-407` 的 `MiniCardStrip.paintEvent`、`window.py:262-278` 的 `LobbyTableWidget._paint_preview_cards`），字号/坐标各自硬编码，JOKER 文案已经出现三种写法。

### 5.3 死代码与孤儿模块

| 位置 | 问题 |
|---|---|
| `src/guandan/smoke.py` | M0a 遗留（45 行），`src/` 内**零引用**，还从 `cli._ai_play` 导入 |
| `src/guandan/tui/widgets/__init__.py` | 只有 docstring + `from __future__ import annotations`，**零引用** |
| `engine/rules/drift.py` | 漂牌占位，恒返回 False，0% 覆盖 |
| `state.py:94` `has_played_ace` | 引擎维护但已不参与任何判定 |
| `state.py:96` `tribute_state` | 写/读/定义三处不一致（见 P1-3） |
| `ai/strategies/dachangsheng.py:49` | `pass_probability_multiplier` 是死配置——`play.py:62` 先检查 `uses_stochastic_pass`，而基类设为 `False` |
| `ai/mcts/search.py::mcts_search` | 完美信息 MCTS，被 `mcts/__init__.py:24,34` 导出，但 `ai/strategies/` 只 import `information_set_search`——生产零调用（**接线的信息泄漏地雷**） |
| `ai/stochastic.py:17` `_is_bomb`、`ai/valuation.py:37` `_is_bomb`、`ai/candidates.py:60` `is_bomb_pattern` | 三份重复实现，规则层已有 `comparator.is_bomb_type` |
| `ai/strategies/dachangsheng.py:49` `pass_probability_multiplier` | 从 profile 读取并存入实例，但 `play.py:65` 的 `getattr` 位于 `:62` 的 `uses_stochastic_pass` 短路之后，且 `ProfessionalStrategy.uses_stochastic_pass = False` → **永不被读取** |

### 5.4 版本号与常量三处硬编码

`pyproject.toml:7` = `__init__.py:14` = `0.8.1b2`，但 `gui/window.py:299` 硬编码 `"BETA  0.8.1"`、`:1547` 硬编码 `"掼蛋 GUI · v0.8.1-beta.2"`、`tui/app.py:14` 硬编码 `"v0.8.1-beta.2 · Public Beta"`、`cli.py:75` 同样。`__version__` **从未被任何人导入**。发一次版必然漂移。

---

## 6. 性能

| 位置 | 问题 | 实测 |
|---|---|---|
| `ai/mcts/determinize.py:179` | 每次模拟都 `copy.deepcopy(state)`，而 history 随后被丢弃 | deepcopy 占单次决策 **41%** |
| `ai/valuation.py:268-282` | 隐藏玩家候选枚举无上限，`structure_cache` 每次调用新建 | 审计实测 27 张手牌单次最高 46.5ms；单次决策 36 次调用中 30 次作用于 >16 张隐藏手牌，合计 45ms ≈ 19% |
| `ai/mcts/search.py:250` | rollout 每轮重算「一手出完」，`find_complete_pattern` | **实测一次 64 次模拟的决策调用 `_legal_finish_pattern` 2386 次**；单次 `find_complete_pattern` 0.10ms(10张)→0.34ms(27张) → 约 **47ms/决策 ≈ 预算的 20%**；cProfile 中 `detect_patterns` 累计 0.613s / 1.133s |
| `ai/mcts/search.py:148,206` | `_expand` 与 `_simulate` 各做一次 deepcopy | 每次模拟两次全状态复制 |
| `engine/replay.py:96-101` | `_snapshot_state` 先 deepcopy 整条 history 再丢弃——注释声称「避免二次方」但实现正是二次方 | 88% 的复制量被浪费；120 事件 73.6ms，随事件数线性增长 → O(n²) |
| `engine/state.py:135-167` | `play_pattern` 每次调用都跑 `detect_patterns` 全量校验 | 实测**真实发牌 27 张 = 0.26–0.89ms**（含逢人配的对子密集手牌最贵）；搜索热路径上每次动作一次 |
| `gui/window.py:899-1031` | 每次 `refresh()` 无条件重置样式（`setStyleSheet`/`set_property`），含每次点牌 | 约 24 次样式失效/刷新 |
| `gui/window.py:791-796` | 回放时间线每步 `setPlainText` 全量重建，且滑杆回环触发两遍 `refresh()` | 长局打开回放冻结 UI 线程 |

**最高 ROI 的四项**：① `_snapshot_state` 手工复制可变字段（Θ(n²)→Θ(n)，一行改动）；② 缓存 `_legal_finish_pattern`（一次决策 2386 次调用 ≈ 47ms）；③ `determinize` 复用牌池 + 手工复制 state；④ 树内隐藏玩家用廉价动作集（按 hand_size 分档）。合计预期把 240ms 预算内的模拟数从 ~30 提到 45-55。

### 6.1 搜索树的一个已复现缺陷：失败的 `_apply_action` 留下「孤儿子节点」

```python
# src/guandan/ai/mcts/information_set.py:340-348
if chosen is None:
    break
child, pattern = chosen
if not _apply_action(sampled_state, actor, pattern):
    break                      # ← child 已经被挂进 node.children，却不回滚
node = child
```

`_expand_action`（`:236-242`）在**应用动作之前**就把 `node.children[key] = child` 写入了树。若 `_apply_action` 失败，该子节点永久留在树里、`visits` 恒为 0；而最终选择 `best = max(action_stats, key=lambda item: (item.visits, item.mean_value))`（`:374`）会在**全部**子节点（含 0 访问的孤儿）里挑最大值。

复现（故意用一个非当前行动者调用公开 API）：12 次模拟后 **7 个根子节点 visits=0 依然存在**，搜索照常返回了一个看起来正常的 `pair_sequence` 决策：

```
searched for player 2 but current_player is 0
  key=(PAIR_SEQUENCE ...)  visits=0 avail=12 mean=0.500
  ...
children with 0 visits still present: 7
selected pattern: Pattern(pair_sequence, rank=12, len=3, ...)
```

生产路径 185 次搜索 0 次触发，所以**当前不是活跃 bug**；但公开 API 缺少 `player == state.current_player()` 的前置校验，失败时返回一个「看似合理」的垃圾决策而非报错。

**建议**：`information_set_search` 入口加契约断言；`_apply_action` 失败时从 `children` 回滚该键。

---

## 7. 测试体系

**429 项测试全部通过**，规则与贡还牌覆盖相当扎实（见附录 B 的覆盖矩阵要点）。问题在**鉴别力**与**缺口**：

### 7.1 恒绿断言（删掉被测代码仍然通过）

```python
# tests/test_card.py:25-31 —— 内部 AssertionError 被同一个 except 吞掉
def test_card_immutable(self):
    c = Card(RANK_5, Suit.HEARTS)
    try:
        c.rank = 99
        raise AssertionError("should be frozen")
    except Exception:          # ← 连自己的断言一起吞了
        pass
```

去掉 `@dataclass(frozen=True)` 这条测试**依然通过**。同类问题还有：

| 位置 | 问题 |
|---|---|
| `tests/test_hypothesis.py:71-79` | `if len({c.rank}) != 1: return` —— 命中概率 1/2197，`max_examples=100` 下断言体约 95.5% 不执行 |
| `tests/test_mcts.py:253,520`、`test_dachangsheng.py:129` | `assert x is None or isinstance(x, Node)` —— 函数只可能返回这两类 |
| `tests/test_ai_benchmark.py:281`、`test_ai_arena.py:103` | 不传 `--fail-*` 时 gate 恒 `passed=True` |
| `tests/test_ai_arena.py:79-83` | `min_win_rate=1.1` 不可达，只验失败字符串 |
| `tests/test_ai.py:423-424,805-806` | `assert costs == sorted(costs)` 用被测的同一个 `estimate_pattern_cost` 重算期望值（镜像实现） |
| `tests/test_visual_qa.py:34-36` | 只断言被测脚本自写 manifest 的格式 |
| `tests/test_ai.py:81` | 名为 `test_make_strategy_3_4_raises`，函数体**不包含任何 `pytest.raises`**，只有 `assert s3.difficulty == 3` |
| `tests/test_ai.py:785-806` | 名为 `test_intermediate_valuation_differs_from_greedy`，AST 检查确认函数体内**从未出现 `IntermediateStrategy`**（全文件 6 处引用都不在这个函数里），也没有与 greedy 的对比 —— 把 `intermediate.py` 改成纯贪心仍会通过 |
| `tests/test_state.py:415-455` | 函数内定义了一个**从未被调用**的 `play_all` 辅助（含「简化：直接 break」注释），随后手工填 `finish_order` 并直接调私有 `_finish_game` —— 真实对局触发结束的路径未覆盖 |
| `tests/test_mcts.py:548-556`、`test_dachangsheng.py:140-149` | 只镜像 `iterations/top_actions/rollout_strategy/hand_threshold/rollout_max_turns`，**恰好漏掉** `time_budget_ms`、`ucb_c`、`max_depth`、`widening_*` —— 把 `time_budget_ms=240` 改成 1 仍全绿 |
| `tests/test_gui_session.py:606,609,619` | 断言常量阈值本身（`minimumWidth() >= 1080`），不断言它被使用 —— 把消费点改成硬编码 20 仍通过 |
| `tests/test_tui.py:275-292` | 用被测模块自己 import 的 `RECOMMENDED_COLUMNS/LINES` 构造边界，常量取值本身错了也测不出 |

### 7.2 高价值缺口

1. **文档承诺的预算从未被断言**：`MCTS_CONFIG["time_budget_ms"] == 240`、profile 的 `420ms`/`max_depth==14`、`CALIBRATED_ITERATIONS == {3:64, 4:96}` —— 全部无测试，`fixed_iteration_strategy_factory` 的 64/96 分支从未执行。
2. **`_action_prior` 与 progressive widening 的行为**（`docs/ai.md:53` 明确承诺）—— 0 引用、0 断言。
3. **搜索层差分泄漏测试**：`tests/test_mcts.py:175-183` 只覆盖 determinize 层；建议补「隐藏手牌互换后 actions 逐位相等」。
4. **TUI/GUI 过牌、键绑定、`closeEvent`、规则屏、难度屏**全无测试（删掉全绿）。`gui/window.py` 覆盖率 **0%**（`tests/test_gui_session.py` 通过子进程运行 GUI，覆盖率钩子不生效）。
5. **存档/历史跨进程锁**与 `StorageBusyError` 路径零直接测试（README:17 已文档化承诺）。
6. **两条未测规则**：`K-A-2-3-4` 非法、第一局随机先手。
7. **无覆盖率门槛**：`pytest-cov` 与 `[tool.coverage.*]` 已声明但 CI 未使用，无 `--cov-fail-under`。

### 7.3 脆弱性

| 位置 | 风险 |
|---|---|
| `tests/test_gui_session.py:839-841` | `QTimer.singleShot(700, loop.quit)` 对上 `setInterval(650)` + `QThreadPool.start(worker)`，**余量只有 50ms**；需覆盖 AI 一轮 + `autosave_after_ai()` 的 fsync。审计中实测到 1 次失败（本机 8 次重跑未复现，但余量确实过窄） |
| `tests/test_tui.py:449-453,490-494` | `for _ in range(20): await asyncio.sleep(0.01)` 等真实线程，超时后静默继续并读取并发写入的 state |
| `tests/test_storage.py:346` | 用 `time.sleep(0.25)` 制造跨进程重叠，第二个进程启动晚于 0.25s 时**无锁也能通过** |
| `tests/test_hypothesis.py:182-199` | 只用 8 个 seed 就断言 `state.finished` |
| `tests/test_visual_qa.py:28` + 5 处 GUI `subprocess.run` | 无超时保护（CI 也无 `timeout-minutes`） |

### 7.4 存储原语「零直接测试」

README:17 把跨进程锁列为特性，但被锁保护的**原语本身没有任何直接测试**：

```
write_json_atomic     0 处引用      storage_lock          0 处引用
StorageBusyError      0 处引用      LOCK_TIMEOUT_SECONDS  0 处引用
os.replace            0 处引用      is_symlink            0 处引用
```

更关键的是，GUI 侧的结算测试把真实事务换成了内存替身：

```python
# tests/test_gui_session.py:35-39
def _profile_transaction(profile: dict):
    def update(mutator):
        return mutator(profile)     # ← 不落盘、不加锁
    return update
# :501, :531, :555  patch("guandan.ui.session.update_profile", side_effect=_profile_transaction(profile))
```

也就是说，「并发下不丢统计」这条保证**没有任何测试真的执行过加锁事务**。第 7.3 节中的符号链接替换、5 秒跨线程阻塞、父目录 fsync 三个缺陷能同时存活，正是这个缺口的结果。

**建议补测**：`write_json_atomic` 的「写失败保留旧档 + 清理临时文件」、`storage_lock` 的同线程重入与跨线程 `StorageBusyError`、损坏 profile 的行为、符号链接目标不被替换。

---

## 8. CI / 发布 / 仓库卫生

| 项 | 现状 | 建议 |
|---|---|---|
| Python 矩阵 | 3.10 + 3.12（缺 3.11/3.13） | `pyproject` 声明 `>=3.10`，至少加 3.13 或明确上限 |
| 静态检查范围 | `ruff` 不含 `packaging/`；`mypy` 排除 `tests/`、`scripts/` | 扩大范围 |
| GUI 测试 | CI 装 `.[dev,gui]` → **真跑，不静默跳过**（已验证本地 23/23 通过） | 保持；但 `ai-arena.yml` 只装 `.[dev]`，GUI 测试会静默全绿 |
| 覆盖率 | 无门槛 | 加 `--cov-fail-under`，并给 `ui/`、`storage/` 设模块级下限 |
| 夜间强度门禁 | 已接线，但只在 schedule/dispatch；16 局下门槛 45% 实际等价于 ≥8/16，**余量仅 1 局** | 提高局数或降低阈值噪声；PR 也应跑一个快速强度冒烟 |
| `--full-match` 整场门禁 | 仅 `workflow_dispatch` | 接入 tag 发布流程（发布前必跑） |
| benchmark `--compare` 门禁 | **完全未接线**（`.github/` 零引用） | 接入 CI 并先重生成基线（当前基线已失效） |
| 依赖安装 | `pip install -e` 而非 `uv sync --frozen` | `uv.lock` 当前形同废纸 |
| 桌面包验证 | `smoke_desktop.py` 除「活 5s」外，还会以 `GUANDAN_PACKAGE_SMOKE=1` 逐档实例化 5 档 AI（做得不错） | PR 阶段可加 PyInstaller 冒烟；`smoke_desktop.py:39-45` 的 `communicate()` 在返回码检查后不可达（死代码） |
| 发布版本校验 | `RELEASE_TAG` 从不与 `pyproject.toml` 比对 | 任意 `v*` tag 可发布版本漂移的产物 |
| 仓库卫生 | `.hypothesis/` **54 个文件已被 git 跟踪**（`.gitignore:36` 虽已忽略但未 `git rm --cached`）；`.gitignore:7` 与 `:42` 重复 `build/` | 清理跟踪的生成物、去重 gitignore |
| `build/` 残留 | 77 个文件，是记录 git 安装时留下的 `build/lib/guandan` **src/ 的逐字节副本**（`diff -rq` 仅差 `.DS_Store`/`__pycache__`/`profiles/README.md`），未跟踪。但 PyInstaller 的 `DEFAULT_WORKPATH` 恰好是 `./build`，而 `desktop-release.yml:51-60` 未传 `--workpath` → `--clean` 会清空该目录。当前无害，但同一目录承担"构建输入"与"构建输出"两种角色容易误判 | 删除残留，并给 PyInstaller 显式传 `--workpath build/pyinstaller` |
| 发布 notes 缺失 | `desktop-release.yml:112-113` 要求 `docs/releases/${RELEASE_TAG}.md` 存在，但 `v0.1.0`…`v0.6.6` 等 21 个历史 tag **没有** notes 文件 → 对这些 tag 手动 dispatch 会直接失败 | notes 缺失时给出明确错误信息或回退到 CHANGELOG 段落 |

---

## 9. 文档一致性

版本主链**一致**（`pyproject.toml:7` = `__init__.py:14` = `v0.8.1-beta.2` = CHANGELOG = release notes = README:3），`CHANGELOG.md:15`「429 项」与实测 `--collect-only` **完全吻合**。以下是不一致项：

| # | 问题 | 证据 |
|---|---|---|
| 1 | **README 基准数字失效** | `README.md:167-169`「91.15 回合 / 炸弹 [0.3,0.4] / 胜场 [2,18]」是 M7 时代的值；`benchmarks/README.md:16-19` 与检入 JSON 是「92.9 / [0.25,0.45] / [3,17]」；我用**同一条命令**复现得「93.15 / [1.0,1.0] / [5,15]」——三个数字全不一样 |
| 2 | README 引用过期基线 | `README.md:173` 引用 M8.1 基线 `v0.8.0b1-mixed-20.json`，而当前已有 M9 的 `ai-v2-style-tuning.json` |
| 3 | 「### M8.1 基准基线」标题误标 | `README.md:171` 的产物是 v0.8.0-beta.1，而 `README.md:115` 的 M8.1 是「事件回放收口」 |
| 4 | README M3 条目写成「AI 4 档」 | `README.md:104`「M3 AI 4 档 IS-MCTS」与全文五档口径不符 |
| 5 | ADR-0001 与代码不符 | `docs/adr/0001-event-flow.md:4` 状态仍是「提议中」却被 `docs/rules.md:207` 当既定参考；事件名 `player_idx`/`Tribute`/`ReturnTribute`/`ResistTribute`/`final_levels` vs 代码的 `player`/`TributeSent`/`TributeReturned`/`TributeResisted`/`team_levels` |
| 6 | ADR-0003 声称预算有单测 | `docs/adr/0003:67-68` 声称 64/240ms 与 96/420ms 有测试，实际**没有** |
| 7 | 档位编号混乱 | `docs/ai.md:11`「档 4 戴长胜」vs `.claude/plan.md:75`「档 5」；`ai/__init__.py:4` 仍写「make_strategy(0\|1\|2)」；`strategies/__init__.py:1` 写「3 档」 |
| 8 | 档位内部命名漂移 | `profiles/README.md:8` 仍写 IS-MCTS，`docs/adr/0003:19` 已改 SO-ISMCTS |
| 9 | 「断点续局恢复」的边界未说明 | README 未提示贡还牌阶段不可恢复（见 P0-1） |
| 10 | `.claude/plan*.md` 与现状冲突 | `plan_m4.md` 把「漂牌决策 / `drift_bonus`」设计为戴长胜档必备行为（`:57,66,226,322,335` 共 5 处），而现行规则明确不启用漂牌（`docs/rules.md:140`、`drift.py:15-16` 恒 False）——`grep -rn drift_bonus src tests packaging` **零命中**；`plan_m5.md` schema `1.0` vs 实际 `3.0`；`plan.md:134`「151 个测试」vs 实际 429 |
| 11 | ADR-0003 取证过度 | `docs/adr/0003:67-68` 声称「单元测试覆盖…预算上限」，实际预算类断言不存在（见 §7.2）。配置值本身（64/240ms/10/深12、96/420ms/12/深14）与 `mcts/__init__.py:38-47`、`profiles/dachangsheng.json:6-16` **完全一致** |
| 11 | 规则文案与控件文案不一致 | `docs/rules.md:177`「≤10 张自动报张数」vs GUI 显示「报双」（`window.py:919-926`） |

---

## 10. 建议的执行顺序

按「先止损、再校准、后优化」排序，每步都是可独立提交的小改动。

### 第一批：数据丢失止损（P0，建议立即）

1. **修 `save_unfinished` 在 pending 期间的静默返回**（`ui/session.py:675-677`）——最低成本：pending 时不删上一局存档；完整方案：把 `_pending_next_game` 落盘。
2. **修 profile 损坏处理**（`storage/profile.py:71-73`）——隔离重命名 + 保留原文件 + 逐字段容错。
3. **TUI 回放异常归一化**（`tui/screens/history.py:139-142` + `tui/screens/replay.py:73-75`）——`except (IllegalPlayError, ValueError)`。
4. **放宽存档「全等即损坏」判据**（`storage/savegame.py:180-182`）——快照优先 + 警告；同时删除或补齐 `tribute_state`。
5. **修存储原语三缺陷**（§P1-7）——符号链接目标解析（`jsonio.py:28`）、锁等待不阻塞 UI 线程且错误消息区分线程/进程（`locking.py:40-42`）、`os.replace` 后 fsync 父目录。

### 第二批：口径校准（P1，影响对外宣称）

6. **修时钟预算超支**：deadline 下沉到 `simulate_state` 循环内；门禁阈值改为 `budget × 1.15`。
7. **文档如实标注搜索实际形态**：`iterations` 标为「仅回归上限」；说明档 3/4 在 `hand_threshold` 以上回落档 2；`--deterministic-search` 与生产的动作序列不可互推。或者——**更好**——把 `hand_threshold` 的闸门放宽/去掉，让档 3/4 真的全程搜索。
8. **树内动作键改可观测键**（`candidates.py:42-49`），让根以下统计真正共享；`SearchResult` 暴露真实树深/节点数。
9. **调参去墙钟项 + 种子三分**（`ai/tuning.py`）。
10. **benchmark 补 `finished` 过滤**，并把 `average_bombs`/`team_wins` 纳入门禁。
11. **重生成 README 基准数字与基线 JSON**，把 `--compare` 接进 CI。

### 第三批：性能（改动小、收益直接）

12. `_snapshot_state` 手工构造快照（`engine/replay.py:96-101`），Θ(n²)→Θ(n)。
13. 缓存 `_legal_finish_pattern`（`ai/mcts/search.py:250`）——一次决策 2386 次调用 ≈ 47ms，是最大单项。
14. `determinize` 复用牌池（`determinize.py:137`）+ 手工复制 state（`:179`）。
15. 树内隐藏玩家改用按 hand_size 分档的廉价动作集（`valuation.py:268-282`）。
16. `play_pattern` 的重复全量校验（`engine/state.py:156-167`）——已由候选生成验证过的 `Pattern` 可跳过重校验。
17. `gui/window.py` 的「内容变化才刷新」（`setStyleSheet`/`set_property` 比较旧值）。
18. 搜索树契约加固：入口断言 `player == state.current_player()`，`_apply_action` 失败时回滚子节点（§6.1）。

### 第四批：结构（逐步偿还）

19. 抽 `AIScheduler`，把 AI 调度从 `GamePage` 移出；`GuandanMainWindow` 只依赖 `can_leave()/on_leave()`。
20. 抽 `paint_card_face()`，消除三份卡面绘制。
21. 把 `displayed_table_actions`/`preserve_completed_trick` 迁回 GUI，恢复 `ui/session.py` 的「前端无关」契约。
22. `window.py` 按页拆分为 `gui/pages/*.py`。
23. 版本号统一从 `guandan.__version__` 取。
24. 删除死代码（`smoke.py`、`tui/widgets/`、完美信息 `mcts_search`、`has_played_ace`、重复的 `_is_bomb`）。

### 第五批：测试加固

25. 消除恒绿断言（§7.1 清单，现 14 条）。
26. 补文档承诺的断言：240/420ms、64/96、`_action_prior`、widening 单调性、搜索层差分泄漏。
27. 补 TUI/GUI 过牌、键绑定、`closeEvent`、规则屏；把 GUI 测试从子进程改为 in-process 以恢复覆盖率；补存储原语测试（§7.4）。
28. 修 50ms 余量的定时器测试（改条件轮询 + 显式超时）。
29. CI 加覆盖率门槛、扩大 ruff/mypy 范围、清理已跟踪的 `.hypothesis/` 与 `build/` 残留。

---

## 附录 A：已证伪／降级的结论

审阅过程中有 4 条来自初步审计的高优先级结论**经复现后不成立或需降级**，如实记录以免误导：

| 初判 | 复现结果 | 修正后结论 |
|---|---|---|
| **P0：`self.game_page` 悬垂引用导致 `closeEvent` RuntimeError + SIGSEGV** | 走完「开局→回菜单→开历史→回菜单」淘汰路径后，`closeEvent` **正常返回**（exit 0）；用 `shiboken6.delete()` 强制销毁主窗口后 `page.deactivate()` 确实抛 RuntimeError，但该场景在 `app.exec()` 阻塞模型的正常使用中不可达 | 降级 **P2**：`game_page` 淘汰后未置空是真实的引用残留/内存泄漏，但「崩溃」不成立 |
| **P1：`load_profile` + `save_profile` 配对可丢更新** | 代码事实成立（两函数各自加锁，调用方无法原子组合），但**当前 src 无任何调用方** | 降级 **P3**：是 API 设计陷阱，非现存缺陷 |
| **P1：`by_difficulty` 嵌套值异常会中止统计事务** | 在磁盘上直接写入 `"by_difficulty": {"2": 5}` 后实测：`update_profile` 正常返回，`_migrate_profile` 完成了归一化，`total_rounds` 等既有统计保留 | **不成立**；该路径不会中止事务，仅丢失那一条 `by_difficulty` 历史。已从 P1-2 正文移除 |
| **P1：TUI 定时器会让「引擎替玩家自动选贡/还牌」** | 核对回调链：`_finish_next_game(None)` 在 `pending_next_game_choice() is not None` 时走 `cancel_next_game()`（`game.py:880-882` → `session.py:531-537`）——**是取消而非代选** | 降级 **P2**：真实问题是缺「当前屏」守卫（贡还弹窗被压到规则屏背后 / 无人类决策时静默推进新局），非规则代选。P1-4 已按实测改写 |
| **P1：AI 后台线程与 UI 线程共享可变 state 会写坏存档** | `human_turn` 确实未包含 `_ai_running`，但所有 `refresh()` 路径都在 AI 阶段被阻断，出牌按钮不会在 worker 写入期间亮起；「存档损坏」路径不可达 | 降级 **P2**：改为「`_ai_running` 未纳入输入锁，与 `save_current_game()` 的意图自相矛盾」，属一致性缺陷 |
| **P1：`test_mcts.py:411` 的 ±0.10 奖金无覆盖** | 同一文件 `:414-420` 断言 `0.766666`，删除该奖金会失败 | **不成立**，该逻辑有覆盖（只是 `:411` 那条因 clamp 饱和而无鉴别力） |

## 附录 B：需求覆盖矩阵（要点）

完整矩阵见审计原始记录。结论分布：`docs/rules.md` 的 30 条口径中 **28 条有对应测试**，缺 `K-A-2-3-4` 负例与「第一局随机先手」；`docs/ai.md` 的 22 条中 **12 条未被断言**，集中在运行时预算、`_action_prior`、progressive widening、候选池代表类、64/96 标定、整场发布门禁；README 的「跨进程锁」只在 profile 上有测试，savegame/history 的锁路径无测试。

## 附录 C：本次审阅产出的可复现证据

审阅过程中的验证脚本均写入 `/tmp`，未修改任何仓库文件。可直接复跑的关键验证：

```bash
# 规则口径全量验证（级牌/连牌/同花顺层级/四王）
# profile 损坏静默归零（改 HOME 到临时目录）
HOME=$(mktemp -d) ./.venv/bin/python -m ...   # 见报告 §3 P0-2

# 贡还牌阶段不可恢复（改 HOME 到临时目录）
HOME=$(mktemp -d) ./.venv/bin/python -m ...   # 见报告 §3 P0-1

# 基准对比门禁（证明炸弹指标漂移未被拦截）
python -m guandan.ai.benchmark --games 20 --difficulties 0,1,2,3 \
  --seed-start 800 --max-turns 2000 --json > current.json
python -m guandan.ai.benchmark --compare benchmarks/v0.8.0b1-mixed-20.json current.json \
  --fail-completion-drop 0.05 --fail-turn-increase 20
```

---

*本报告基于只读审阅，未改动 `src/`、`tests/`、`docs/` 中任何文件。*
